#include "main.h"
/**
 * _islower - function for checking lowercase characters
 *
 * @c: lowercase charecter that needs to be checked
 *
 * Return: 1 if c is a lowercase, 0 if c is not a lowercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
